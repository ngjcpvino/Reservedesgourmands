# Code Google Apps Script — JAMAIS sur GitHub.
# Contient la clé API + l'ID du Sheet. Vit dans le dossier pour copier-coller
# dans l'éditeur Apps Script. Le dépôt GitHub est public.
*.gs

# Archives de l'ancien build (GAS, HTML, CSS, JS) — gardées en local, jamais publiées.
archive-gas/
